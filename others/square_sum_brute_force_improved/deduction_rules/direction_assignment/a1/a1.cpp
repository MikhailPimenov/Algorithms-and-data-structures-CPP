#include "a1.h"



static bool has_one_entering_and_one_undirected_edge(int current_vertex, const Graph_t& required) {
	int entering_counter = 0;

	for (const auto& [vertex, neighbors] : required) {

	}
}


void a1(int current_vertex, Graph_t& required) {
	//if (has_one_entering_and_one_undirected_edge(current_vertex, required))
		//change_undirected_to_leaving_edge(current_vertex, required);

	//if (has_one_leaving_and_one_undirected_edge(current_vertex, required))
		//change_undirected_to_entering_edge(current_vertex, required);
}